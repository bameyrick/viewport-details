{
  "name": "viewport-details",
  "version": "1.0.0",
  "description": "'Get cached viewport information each animation frame'",
  "main": "index.js",
  "author": "Ben Meyrick <bameyrick@gmail.com>",
  "license": "MIT",
  "scripts": {
		"lint": "tslint --project src/*.ts",
		"lint-staged": "lint-staged",
    "precommit": "lint-staged",
    "post-commit": "prepublishOnly",
		"tslint-check": "tslint-config-prettier-check ./tslint.json",
		"prepublishOnly": "npm run lint && tsc -p ./ --outDir dist/"
	},
  "devDependencies": {
    "husky": "^0.14.3",
    "lint-staged": "^5.0.0",
    "prettier": "^1.8.2",
    "tslint": "^5.8.0",
    "tslint-config-prettier": "^1.6.0",
    "tslint-plugin-prettier": "^1.3.0",
    "typescript": "^2.6.1"
  },
  "dependencies": {
    "tick-manager": "^1.0.2"
  }
}
